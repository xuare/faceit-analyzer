services:
  - type: web
    name: faceit-analyzer
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: FACEIT_API_KEY
        sync: false
      - key: NODE_VERSION
        value: 20
